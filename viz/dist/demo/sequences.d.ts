import { Sequence, Points, PointMetadata } from '../src/data';
export declare function makeSequences(points: Points[], metadata: PointMetadata[], nSequencesPerLabel?: number, sequenceLength?: number): Sequence[];
